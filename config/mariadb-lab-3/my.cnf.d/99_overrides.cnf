[mysqld]

character-set-client-handshake = FALSE
character-set-server = utf8mb4
collation-server = utf8mb4_unicode_ci
slave_parallel_mode = conservative
# proxy_protocol_networks = "10.177.0.0/16"
sync_binlog=1
# wait_timeout=43200

[mysql]
default-character-set = utf8mb4
